@page
@model CirAnime.Pages.UploadEntries.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UploadEntry[0].Title)
            </th>
            <th>
                Status
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.UploadEntry) {
        <tr id="@item.ID">
            <td>
                <a href="/v/@(item.MediaInfo.ID + ".json")">@item.Title</a>
            </td>
            <td>
                @foreach (var job in item.ProcessingJobs)
                { 
                    <p>@job.Quality : @job.Progress</p>
                }
            </td>
            @*<td>
                <ul>
                    @foreach (var source in item.MediaInfo.sources)
                    {
                        <li>@source.url </li>
                    }
                </ul>
                @Html.DisplayFor(modelItem => item.MediaInfo.sources)
            </td>
            <td>
                
                @{ var a = item.MediaInfo.ID + ".json";}
                 <a href="@("https://www.cirani.me/v/" + @item.MediaInfo.ID + ".json")">@item.MediaInfo.title</a>
            </td>*@

            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
